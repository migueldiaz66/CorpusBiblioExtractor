# Options — Countries' Collaboration World Map (`collabWorldMap`)

> Opciones extraídas de biblioshiny 5.4.1 (`inst/biblioshiny/ui.R`, líneas 10946–11114). Ruta de menú: SYNTHESIS › Social Structure › Countries' Collaboration World Map.

## Botones (encabezado)
- **Run / Apply** (`applyWM`)
- **Report** (`reportCOLW`)
- **Export / Screenshot** (`CCplot.save`)

## Method Parameters (box colapsable, `collapsed = FALSE`)

### Filtering Options (subgrupo)
- **Min Edges** (entero · `WMedges.min`) — step: 1 · default: 2.

## Graphical Parameters (box colapsable, `collapsed = FALSE`)

### Edge Settings (subgrupo)
- **Edge Size** (entero · `WMedgesize`) — min: 0.1, max: 20 · default: 5.

## Export
Sin botones de exportación (download) en `ui.R`. La exportación se realiza desde el botón de encabezado **Export / Screenshot** (`CCplot.save`).
