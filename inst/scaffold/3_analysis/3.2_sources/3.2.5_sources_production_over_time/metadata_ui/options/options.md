# Options — Sources' Production over Time (`sourceDynamics`)

> Opciones extraídas de biblioshiny 5.4.1 (`inst/biblioshiny/ui.R`, líneas 3466–3594). Ruta de menú: ANALYSIS › Sources › Sources' Production over Time.

## Botones (encabezado)
- **Run / Apply** (`applySOGrowth`)
- **Report** (`reportSD`)

## Main Configuration
- **Occurrences** (dropdown · `cumSO`) — opciones: Cumulate (`Cum`), Per year (`noCum`) · default: Cumulate (`Cum`).
- **Number of Sources** (slider · `topSO`) — rango: min 1, max 50, step 1 · default: c(1, 5) (rango seleccionado 1–5).

## Export
- **Plot (download)** (`SDplot.save`)
