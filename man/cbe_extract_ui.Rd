% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbe_extract_ui.R
\name{cbe_extract_ui}
\alias{cbe_extract_ui}
\title{Extract the biblioshiny UI metadata from source (via the R AST)}
\usage{
cbe_extract_ui(src = NULL, out = NULL)
}
\arguments{
\item{src}{biblioshiny source directory. Default:
\code{system.file("biblioshiny", package = "bibliometrix")} (the installed package).}

\item{out}{Optional directory; if given, writes \code{ui_metadata.json} there.}
}
\value{
(invisibly) the metadata list.
}
\description{
Parses \code{ui.R} of the installed (or given) biblioshiny and returns a structured
list: the sidebar \code{menu} tree, one entry per \code{tabItem} page with its \code{options}
(controls, nesting-aware: box \code{group} + \code{conditionalPanel} condition) and \code{tabs}
(output panels), plus the \code{helpContent()} keys.
}
