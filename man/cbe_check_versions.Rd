% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbe_versions.R
\name{cbe_check_versions}
\alias{cbe_check_versions}
\title{Check runtime versions against the tested, pinned baseline}
\usage{
cbe_check_versions(strict = FALSE)
}
\arguments{
\item{strict}{If \code{TRUE}, raise an error instead of a warning on a mismatch.}
}
\value{
(invisibly) \code{TRUE} if everything matches the baseline, \code{FALSE} otherwise.
}
\description{
CorpusBiblioExtractor is pinned (see \code{renv.lock}) to the dependency versions it
was tested with. This warns — or, with \code{strict = TRUE}, errors — when the
installed R or \code{bibliometrix} differs, because \code{biblioshiny}'s UI (mirrored by
the runner and parsed by the extractor) changes between versions. It is called
automatically at the start of \code{\link[=cbe_run]{cbe_run()}}.
}
