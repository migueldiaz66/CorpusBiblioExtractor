% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbe_run.R
\name{cbe_run}
\alias{cbe_run}
\title{Run the exhaustive biblioshiny sweep over a bibliographic corpus}
\usage{
cbe_run(
  bib = NULL,
  only = NULL,
  resume = NULL,
  seed = 42L,
  runs_root = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{bib}{Path to a BibTeX corpus file (required unless \code{dry_run = TRUE}).}

\item{only}{Optional comma-separated id substrings restricting which sections run.}

\item{resume}{Optional run timestamp (UTC, colon-free) to resume; assets already
marked \code{ok} are skipped and \code{error}/missing ones retried.}

\item{seed}{Integer RNG seed for reproducibility of stochastic steps (default 42).}

\item{runs_root}{Directory under which the dated run directory is created
(default \code{file.path(getwd(), "runs")}).}

\item{dry_run}{If \code{TRUE}, list scenarios and expected assets without computing.}
}
\value{
(invisibly) the run directory path.
}
\description{
Runs every section x Main-Configuration combination of biblioshiny 5.4.1 by
calling \code{bibliometrix} directly (no GUI, no Python) and materialises every
output asset to \verb{<runs_root>/<UTC>/<section>/<combination>/}, plus a
per-scenario \verb{_timing.csv}, a resume \verb{_manifest.json}, a \verb{_corpus_info.json},
and a self-contained per-run \code{index.html}.
}
